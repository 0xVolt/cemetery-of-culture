fout = open('myfile.txt', 'w')

string = "A teacher answers. There is no need to panic. Any kind of questions are welcome. There is a reason. All letters andnumbers are allowed in the password."

fout.write(string)
fout.close()