Days = {"Saturday", "Sunday", "Monday", "Tuesday"} # Sets use {} rather than []
test = {} # Even though we use curly braces, the 'empty set' is treated as dict

# While printing, the order in which its printed
# can change
print(Days)
print(type(Days))
print(type(test))